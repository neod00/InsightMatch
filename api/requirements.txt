# 버전 고정(pin): 배포마다 다른 버전이 설치되어 예기치 않게 동작이 바뀌거나
# 취약한 버전이 유입되는 것을 막는다. 아래는 로컬에서 테스트를 통과한 조합이다.
# 업데이트 시 `pip-audit` 등으로 취약점을 확인한 뒤 값을 올릴 것.

flask==2.3.3
Werkzeug==3.1.3          # Flask의 전이 의존성 — 명시 고정(과거 다수 CVE 이력)
flask-cors==4.0.0
flask-sqlalchemy==3.1.1
SQLAlchemy==2.0.44
psycopg2-binary==2.9.11
python-dotenv==1.0.0
google-generativeai==0.8.5
requests==2.32.4        # CVE-2024-35195 대응 (2.31.0 → 2.32.4)
beautifulsoup4==4.12.2
pyjwt==2.10.1
reportlab==4.4.3
openai==2.24.0
python-docx==0.8.11
